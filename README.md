# Работа с массивами в C
## 📋 Лабораторная 14
### Задания: 
1. Заполнение массива значениями из функций F.
2. Печать массива 
3. Вычисление  суммы элементов массива в заданном диапазоне индексов 
4. Поиск первого вхождения элемента в массиве 
5. Поиск индекса наименьшего элемента, кратного заданному числу k
6. Поиск индекса наименьшего элемента, большего заданного значения A

## 📋 Лабораторная 16
### Задания: 
1. Заполнение массива значениями из функции Func(x)
2. Печать массива
3. Вычисление количества элементов массива
4. Печать массива
5. Удаление элемента с заданным номером.
6. Вставка в случайную позицию.

## 📁 Структура проекта
14-16/                                         
├── app.c          # Главная программа с лабораторными работами       
├── arrays.h       # Заголовочный файл с объявлениями функций                      
├── arrays.c       # Реализация функций для работы с массивами               
├── README.md      # Документация проекта           
└── .gitignore                   


## 🔧Реализация функций
1. double F(double x)  
- Назначение: Вычисление значения кубической функции f(x) = x³ - 4x² + 2  
- Параметры: x - аргумент функции  
- Возвращает: значение функции в точке x                          

2. double* full_elements(double* ptr_array, int n)  
- Назначение: Заполнение массива значениями функции F(x) с шагом 0.1, начиная с x=1.0  
- Параметры: ptr_array - указатель на массив, n - размер массива
- Возвращает: указатель на заполненный массив  

3. int put_elements(double* ptr_array, int n)  
- Назначение: Вывод всех элементов массива в консоль с форматированием  
- Параметры: ptr_array - указатель на массив, n - размер массива   

4. double* calc_elements(double* ptr_array, int n)  
- Назначение: Инверсия знаков всех элементов массива (умножение на -1)
- Параметры: ptr_array - указатель на массив, n - размер массива
- Возвращает: указатель на изменённый массив

5. double sum_elements(double* ptr_array, int begin, int end)  
- Назначение: Вычисление суммы элементов массива в заданном диапазоне индексов
- Параметры: ptr_array - указатель на массив, begin - начальный индекс, end - конечный индекс
- Возвращает: сумму элементов

6. int find_element(double* ptr_array, int n, double element)  
- Назначение: Поиск первого вхождения элемента в массиве
- Параметры: ptr_array - указатель на массив, n - размер массива, element - искомое значение
- Возвращает: индекс найденного элемента или -1, если не найден

7. int function(double* arr, int n, double k)  
- Назначение: Поиск индекса наименьшего элемента, кратного заданному числу k
- Параметры: arr - указатель на массив, n - размер массива, k - делитель
- Возвращает: индекс найденного элемента или -1, если не найден

8. int findDZ(double* arr, int n, double A)  
- Назначение: Поиск индекса наименьшего элемента, большего заданного значения A
- Параметры: arr - указатель на массив, n - размер массива, A - пороговое значение
- Возвращает: индекс найденного элемента или -1, если не найден
